# agent-ai
This project is an AI-powered Research Agent built using LangChain, Groq API, and LLaMA 3.1 model. The agent can search the web, retrieve information from Wikipedia, generate structured research summaries, and save the research output to a text file.  The system uses tool-calling agents, structured output parsing using Pydantic.
